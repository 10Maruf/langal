# 🚀 Marketplace দ্রুত Setup করুন

## সমস্যা

Marketplace এ listing post করলে logout করার পর চলে যাচ্ছে কারণ database এ save হচ্ছে না।

## কারণ

Database এ `marketplace_categories` table খালি আছে।

## সমাধান (২ মিনিট)

### ধাপ ১: XAMPP শুরু করুন

1. XAMPP Control Panel খুলুন
2. **Apache** এবং **MySQL** start করুন

### ধাপ ২: Database Setup

#### **Option A: phpMyAdmin দিয়ে (সহজ)**

1. Browser এ যান: `http://localhost/phpmyadmin`
2. Left sidebar থেকে `langol_krishi_sahayak` database select করুন
3. উপরে **SQL** tab এ click করুন
4. নিচের SQL copy করে paste করুন:

```sql
INSERT IGNORE INTO `marketplace_categories`
(`category_id`, `category_name`, `category_name_bn`, `description`, `icon_url`, `is_active`, `sort_order`)
VALUES
(1, 'crops', 'ফসল ও শাকসবজি', 'সব ধরনের ফসল, শাকসবজি এবং কৃষিপণ্য', '🌾', 1, 1),
(2, 'machinery', 'যন্ত্রপাতি', 'কৃষি যন্ত্রপাতি, ট্রাক্টর, পাওয়ার টিলার ইত্যাদি', '🚜', 1, 2),
(3, 'fertilizer', 'সার ও কীটনাশক', 'রাসায়নিক সার, জৈব সার, কীটনাশক', '🧪', 1, 3),
(4, 'seeds', 'বীজ ও চারা', 'উন্নত জাতের বীজ, চারা, কলম', '🌱', 1, 4),
(5, 'livestock', 'গবাদি পশু', 'গরু, ছাগল, মুরগি, হাঁস ইত্যাদি', '🐄', 1, 5),
(6, 'tools', 'হাতিয়ার', 'কোদাল, কাস্তে, লাঙল ও অন্যান্য হাতিয়ার', '🔧', 1, 6),
(7, 'other', 'অন্যান্য', 'অন্যান্য কৃষি সম্পর্কিত পণ্য ও সেবা', '📦', 1, 7);
```

5. **Go** button এ click করুন
6. Success message দেখবেন: "7 rows inserted"

#### **Option B: MySQL Command Line দিয়ে**

```bash
mysql -u root -p langol_krishi_sahayak < setup_marketplace.sql
```

### ধাপ ৩: Backend Server চালু করুন

PowerShell বা Terminal খুলে:

```bash
cd langal-backend
php artisan serve
```

দেখবেন: `Server running on [http://127.0.0.1:8000]`

### ধাপ ৪: Frontend Server চালু করুন (নতুন terminal এ)

```bash
npm run dev
```

দেখবেন: `Local: http://localhost:5175/`

### ধাপ ৫: Test করুন

1. Browser এ যান: `http://localhost:5175`
2. Farmer হিসেবে login করুন
3. "কেন্দ্রীয় বাজার" menu তে click করুন
4. "বিজ্ঞাপন দিন" button click করুন
5. Form fill করুন:
   - **ধরন:** বিক্রি
   - **ক্যাটেগরি:** 🌾 ফসল ও শাকসবজি
   - **শিরোনাম:** তাজা ধান
   - **বিবরণ:** BRRI-28 জাত
   - **দাম:** 28
   - **স্থান:** কুমিল্লা
   - **যোগাযোগ:** 01812567890
6. "বিজ্ঞাপন দিন" click করুন
7. **এখন logout করে আবার login করুন**
8. দেখবেন আপনার listing সেখানে আছে! ✅

## Verify Database এ Data আছে কিনা

phpMyAdmin এ SQL run করুন:

```sql
-- Categories check
SELECT * FROM marketplace_categories;

-- Listings check
SELECT * FROM marketplace_listings;
```

## যদি এখনও সমস্যা হয়

### Chrome DevTools খুলে Console check করুন (F12)

1. Network tab এ যান
2. XHR filter select করুন
3. Listing create করুন
4. `marketplace` request এ click করুন
5. Response দেখুন

**সফল হলে দেখবেন:**

```json
{
  "success": true,
  "data": {
    "listing_id": 1,
    "title": "তাজা ধান",
    ...
  }
}
```

**Error হলে দেখবেন:**

```json
{
  "success": false,
  "errors": {...}
}
```

### সাধারণ Errors:

**Error 1:** "seller_id does not exist"
**Fix:** Login করা আছে কিনা check করুন

**Error 2:** "category_id does not exist"  
**Fix:** উপরের SQL আবার run করুন

**Error 3:** Network request failed
**Fix:** Backend server running আছে কিনা check করুন

---

## ✅ Setup Complete!

এখন থেকে:

- ✅ Listing create করলে database এ save হবে
- ✅ Logout করলেও data থাকবে
- ✅ অন্য user দেখতে পারবে
- ✅ Edit/delete করতে পারবেন

**কোন সমস্যা হলে console error দেখান!**
